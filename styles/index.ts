import { colors, fontSizes, fontWeights, fonts } from './foundation'

import { extendTheme } from '@chakra-ui/react'
import styles from './globals'

export default extendTheme({
  colors,
  fonts,
  fontSizes,
  fontWeights,
  styles,
})